# 用 AI 工具一句话生成分享网址

> 🚀 不懂技术、不想注册 GitHub？把项目文件夹交给支持"网页发布"的 AI 工具（WorkBuddy、Claude.ai、ChatGPT 等），说一句话就能拿到 HTTPS 链接发给亲友。

## 为什么推荐这种方式

- **零门槛**：不需要理解 Git、域名、Pages、服务器等概念。
- **几分钟拿到链接**：AI 自动上传 + 部署 + 返回 HTTPS URL。
- **支持自定义域名**（部分工具）：可以把 `xxx.app.workbuddy.link` 换成自己的域名。
- **可重复更新**：重新对话说"重新发布"即可更新内容，旧链接会自动跳到新版。

## 推荐工具（截至 2026 年 8 月）

| 工具 | 是否需注册 | 国内访问 | 免费额度 | 推荐度 |
| --- | --- | --- | --- | --- |
| WorkBuddy | 是（手机号 / 微信） | ✅ 国内直连 | 每天有免费发布次数 | ⭐⭐⭐⭐⭐ |
| Claude.ai（Projects） | 是（海外手机号） | ⚠️ 需稳定海外网络 | 含代码沙箱但发布受限 | ⭐⭐ |
| ChatGPT | 是（海外手机号） | ⚠️ 需稳定海外网络 | 需手动下载再用 Pages 部署 | ⭐⭐ |
| 通义千问 / 文心一言 | 是 | ✅ | 大多仅支持代码片段，无完整发布 | ⭐ |

## 推荐流程（以 WorkBuddy 为例）

### 步骤 1：上传项目

1. 把项目文件夹压缩成 zip（或保持原样）。
2. 打开 WorkBuddy 对话，新建会话。
3. 拖入 zip（或文件夹路径），说："请帮我检查这个项目是否能直接发布为网页。"

### 步骤 2：一句话发布

确认 AI 能正常加载项目后，输入：

```text
请把这个项目发布为在线应用，生成一个可以分享的 HTTPS 链接。我需要发给国内亲友，要求国内可直接打开且加载速度可接受。发布完成后告诉我链接和如何更新内容。
```

### 步骤 3：获取链接

AI 通常会返回形如：

```text
https://xxxxx.app.workbuddy.link/
```

复制这个链接，发给亲友即可。**不要发** `localhost`、`file://` 或 `127.0.0.1` 之类的本地地址（亲友打不开）。

### 步骤 4：更新内容

以后改完 `public/config.js` 后，回到同一个 WorkBuddy 会话（或新开会话引用同一个项目），说：

```text
我已经更新了 public/config.js，请重新发布，旧链接保留但内容刷新成新版。
```

AI 会重新触发发布，**同一链接**自动指向新内容，无需通知亲友换地址。

## 部署前必读：版权

⚠️ 模板默认使用《胡闹厨房 2》原版游戏画面作为视觉素材。**仅作个人演示与测试可以接受**；正式婚礼或任何公开传播前，请务必替换为原创或已授权素材，否则可能：

- 收到权利人（Team17 / Ghost Town Games）下架要求；
- 被微信 / 朋友圈屏蔽；
- 在公开论坛引发版权争议。

快速替换清单见 [THIRD_PARTY_ASSETS.md](./THIRD_PARTY_ASSETS.md)。

## 排错速查

### AI 反馈"项目结构无法识别"

- 确认上传的是解压后的**文件夹**而非 zip 本身；
- 确认 `public/index.html` 在项目的 `public/` 目录中（不是更深的嵌套子目录）；
- 让 AI 先检查 `public/`，确认 `config.js`、`assets/`、`style.css`、`app.js` 都存在。

### 部署后页面空白

- 路径问题：浏览器 F12 → Console 查看 404 资源；
- 让 AI 检查 `public/config.js` 是否有语法错误（多余的逗号、未闭合的引号）。

### 国内访问慢或打不开

- WorkBuddy 默认国内可直连，若仍慢可在对话中说"请检查是否启用国内 CDN"；
- 其他 AI 工具生成的链接通常基于海外服务器，国内访问可能慢——建议改用 WorkBuddy 或自管 GitHub Pages + CDN。

### 朋友看到的是旧版本

- AI 工具的更新有几分钟生效延迟；
- 让对方在浏览器设置中"清除网站数据"后重新打开；
- 微信分享卡片有独立缓存，可让对方把链接发给"文件传输助手"打开一次，再回到聊天刷新。

## 想自己掌控？试试 GitHub Pages

如果你想完全掌控部署过程（包括自定义域名、版本回滚、永久地址），可以走 GitHub Pages：

1. 在 GitHub 创建新仓库（public），上传项目所有文件。
2. Settings → Pages → Branch 选择 `main`，目录 `/ (root)`，保存。
3. 等待几分钟，访问 `https://你的用户名.github.io/仓库名/` 即可。

详细步骤见 [README § 发布成网址](./README.md#发布成网址-两种方式选一种)。

国内访问 GitHub Pages 可能较慢或不稳定；建议再加一个 WorkBuddy 国内镜像作为冗余。
